# دليل تسليم المشروع للمهندس (Handover Document - markAgency)

هذا المستند موجه للمهندس المستلم للمشروع لضمان استكمال وتأكيد عمل كافة الخصائص الحالية والمستقبلية لنظام إدارة التسويق **markAgency / markPocket**.

---

## 1. إصلاح الشعار (Logo Component Fix)
- **المشكلة السابقة:** كان الشعار يظهر مقصوصاً (نصف الشعار فقط) بسبب اقتصاص الصورة داخل `width` مربع في مكون `Logo.tsx`.
- **الإصلاح الذي تم:** 
  - تم تحديث مكون `Logo.tsx` في مشروعي `client-front` و `agency-front`.
  - أصبح الشعار الكامل يظهر بوضوح وبأبعاده الطبيعية عند فتح القائمة (`hideText = false`).
  - عند تصغير القائمة الجانبية (`hideText = true`) يتم عرض أيقونة الشعار المربعة بوضوح وبدون تشويه.

---

## 2. الربط مع منصات التواصل الاجتماعي (Social Media Integration)
**المطلوب:** التأكد من سلامة الربط البرمجي والاستجابة مع مختلف المنصات (Meta, Instagram, LinkedIn, X/Twitter, TikTok).

### المهام المطلوب تنفيذها وتأكيدها:
1. **مصادقة OAuth 2.0:**
   - توفير مسارات تسجيل الدخول بـ OAuth لكل منصة في الخلفية (Backend).
   - إدارة رموز الوصول الطويلة المدى (Long-Lived Access Tokens) وتجديد الرموز تلقائياً (Token Refresh Worker).
2. **إدارة الحسابات والصفحات:**
   - ربط حسابات الأعمال (Business Pages) وحسابات إنستغرام التجارية (Instagram Business Accounts).
   - تخزين بيانات الاعتماد المشفرة في قاعدة البيانات (Encrypted Tokens).

---

## 3. نظام جدولة المنشورات والنشر التلقائي (Scheduling & Automated Posting System)
**المطلوب:** بناء وتأكيد نظام جدولة دقيق للمنشورات يضمن النشر في الوقت والمكان المحددين بالضبط.

### المهام المطلوب تنفيذها وتأكيدها:
1. **طابور المهام في الخلفية (Job Queue System):**
   - استخدام مكتبة مثل **BullMQ (Redis)** أو **Agenda (MongoDB)** لإدارة مهام النشر المجدولة.
   - معالجة حالات الفشل وإعادة المحاولة في حال انقطاع الشبكة أو رفض API المنصة.
2. **إدارة المناطق الزمنية (Timezones):**
   - تحويل وقت النشر المختار من قبل المستخدم إلى `UTC` في قاعدة البيانات لضمان دقة وقت التنفيذ.
3. **معالجة وسائط الميديا (Media Pipeline):**
   - قص وضبط مقاسات الصور والفيديوهات لتناسب شروط كل منصة (Reels 9:16, Feed 1:1 أو 4:5).
4. **توصية ميزة مخصصة (Best Time to Post):**
   - خوارزمية مقترحة لتحليل تفاعل المتابعين واقتراح أفضل الأوقات للنشر لكل قناة.

---

## 4. الإعلانات المأجورة والتتبع وتحديثات البث المباشر (Media Buying, Click Tracking & Live Monitoring)
**المطلوب:** الكشف عن المنشورات الممولة (Sponsors)، تتبع النقاط والزيارات، ومتابعة الأداء والبث المباشر.

### المهام المطلوب تنفيذها وتأكيدها:
1. **الكشف عن الإعلانات الممولة (Sponsor / Media Buying Detector):**
   - الربط مع **Meta Marketing API** و **Google Ads API** للتعرف تلقائياً على الحملات الترويجية والمنشورات الممولة المرتبطة بالحساب.
   - جلب بيانات الإنفاق اليومي (Ad Spend), سعر النقرة (CPC), ومعدل الظهور (Impressions).
2. **نظام تتبع النقرات والروابط (Click & Conversion Tracking):**
   - إنشاء محول روابط تلقائي (UTM Builder & Shortener) لكل منشور ممول أو مجدول.
   - تسجيل كل نقرة (Clicks) وجلب المصدر والتأكد من تحليلات التحويلات (Conversions).
3. **متابعة البث المباشر والأداء اللحظي (Live Stream & Real-time Analytics):**
   - تفعيل الـ Webhooks من شبكات التواصل للحصول على إشعارات لحظية أثناء البث المباشر أو عند التعليقات والتفاعلات.
   - عرض مؤشرات التفاعل المباشر عبر الـ WebSockets في الواجهة (`agency-front` & `client-front`).

---

## 5. نظام البوت الذكي لاستقبال الطلبات والمبيعات من بياناتك الخاصة (Custom Data AI Order & Conversation Agent)
**المطلوب:** بناء بوت مبيعات ذكي يقوم بالرد بأسلوب ونبرة العلامة التجارية ومن واقع بيانات المنتجات/الخدمات الخاصة وتجمّع الطلبات تلقائياً.

### الأركان الأساسية للنظام:
1. **نبرة الرد والأسلوب المخصص (Brand Tone & Persona Engine):**
   - تحديد تعليمات النظام (System Instructions) لتحديد نبرة الرد (رسبي، ودود، لهجة محلية) وتحديد قواعد عدم التجاوز.
2. **الربط مع قاعدة البيانات والكتالوج الخاص (Custom Knowledge Base / RAG):**
   - تزويد الـ AI ببيانات الكتالوج والخدمات والأسعار والأسئلة الشائعة مباشرة من قاعدة البيانات (`catalog.json` / `ai_settings.json`).
3. **تجمع الطلبات التلقائي عبر الـ Function Calling (AI Tool Use / Order Creation):**
   - استخدام خاصية الـ **Function Calling** في Gemini / OpenAI للتعرف التلقائي عندما يرغب العميل في الشراء.
   - استخراج بيانات العميل (الاسم، رقم الهاتف، الباقة/المنتج المطلوب، الملاحظات) وتنفيذ دالة `createOrder` لحفظ الطلب فوراً في قاعدة البيانات (`orders.json`).

---

## 6. ميزات مقترحة لرفع قيمة النظام (Recommended Advanced Features)
1. **صانع المحتوى بالذكاء الاصطناعي (AI Content & Caption Generator):**
   - دمج نموذج Gemini / OpenAI لكتابة نصوص المنشورات واقتراح الهاشتاجات وتوليد أفكار المحتوى.
2. **صندوق الرسائل والتعليقات الموحد (Unified Social Inbox):**
   - لوحة واحدة لإدارة جميع التعليقات والرسائل الخاصة الواردة من فيسبوك، إنستغرام، وتويتر.
3. **تقارير الأداء الآلية (Automated PDF/Excel Reports):**
   - توليد تقارير شهرية وأسبوعية بصيغ PDF لمشاركتها مع العملاء تحتوي على نِسَب النمو والأداء والإعلانات.
4. **نظام الموافقة على المحتوى (Client Approval Workflow):**
   - تمكين العملاء من مراجعة المنشورات المجدولة والموافقة عليها أو طلب تعديلها قبل النشر الفعلي.

---

## 7. معلومات تشغيل الواجهات (Quick Start Commands)
```bash
# تشغيل واجهة الوكالة (Agency Portal)
cd agency-front
npm run dev

# تشغيل واجهة العميل (Client Portal)
cd client-front
npm run dev

# تشغيل الباك إند (Backend Server)
cd backend
npm start
```
